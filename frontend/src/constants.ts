/**
 * Constants for confidence score thresholds.
 */

// Confidence score thresholds for UI display
export const CONFIDENCE_THRESHOLD_HIGH = 0.7; // High confidence threshold (green)
export const CONFIDENCE_THRESHOLD_MEDIUM = 0.4; // Medium confidence threshold (yellow)

/**
 * Pagination constants
 */
export const PAGINATION_LIMIT = 10; // Default number of items per page

